FROM node:22-alpine
WORKDIR /app
COPY server/package*.json ./server/
RUN cd server && npm ci --omit=dev
COPY server ./server
ENV SERVICE=escrow PORT=4003
EXPOSE 4003
CMD ["node","server/services/escrow.service.js"]
