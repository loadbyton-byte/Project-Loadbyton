# Differential Review Report — Loadbyton development @ 8be1c5f

Date: 2026-09-08. Scope: full Phases 0–8 stack on `development`
(Phases 0–6b via upstream PRs #22–#32, Phases 7a/7b/8 + Change 20/21/30 as
direct commits), verified against `The-Loadbyton-Execution-Roadmap.html`
(31 changes, Phases 0–8 + parallel track). Method: risk-first differential
review (triage → code → tests → blast radius → adversarial), two parallel
research passes (dead-code audit, roadmap traceability) plus direct
verification of every HIGH-risk path. Confidence: HIGH on reviewed paths;
explicitly NOT covered: `e2e/`, `k6/`, infra/terraform, real-device iOS.

## 0. Push & CI status (ship lens)

- `development` pushed in full: origin/development == 8be1c5f (single
  canonical repo; old `shamhar07-max` URL redirects to `loadbyton-byte`).
- Upstream PRs #22–#32 all MERGED; #28–#32 auto-closed by this push, their
  head commits reachable via the integrated merge history.
- CI was RED on push (vite build syntax error, see §1) → fixed → **CI green
  (success) on 5e6b1bc**. Server tests + tsc + openapi-validate passed even
  on the red run, which independently confirms the backend stack.

## 1. Errors found and fixed during this review

| # | Severity | Finding | Fix (commit) |
|---|----------|---------|--------------|
| 1 | CRITICAL (CI red) | `web/src/pages/Dashboard.jsx:151` — origin merge left `});` + orphaned `}, postJobIdempotencyKeyRef.current);`, vite build failed, CI red | pass key as 2nd arg to `api.createJob` (5e6b1bc). Lesson: no build was run after the origin merge |
| 2 | HIGH (authz) | BROKER satisfied CARRIER bid guard via `roleSatisfies` → could bid with no fleet, incl. self-dealing on own postings | explicit BROKER reject in bid route + regression test (8be1c5f) |
| 3 | HIGH (authn) | WhatsApp webhook skipped signature when `WHATSAPP_APP_SECRET` unset — even with sending live, allowing forged "Delivered" confirmations | fail closed when `isConfigured()` (8be1c5f) |
| 4 | MEDIUM | `POST …/direct-assign` duplicate line (`const bidId` after `let bidId`) — would have been SyntaxError/ReferenceError crash on the route | removed; race now returns friendly 409 (8be1c5f) |
| 5 | MEDIUM | Insurance bind double-submit race → raw 500 on UNIQUE | friendly 409 (8be1c5f) |
| 6 | MEDIUM | Origin's `job.service.js` masking had a duplicated ternary (`? A ? B : C`) — always truthy, silently dropped `ancillary_charges: []` masking while OPEN | kept single full-mask line in merge 5c83f3f |
| 7 | LOW | `openapi.yaml` role enum `[SHIPPER,CARRIER]` rejected real roles; docType enum stale | corrected to 5 roles + 12 doc types (8be1c5f) |
| 8 | LOW | Insurance test name claimed 503 coverage it didn't have; harness port collision + health-probe gap masked wrong-server testing | renamed + real broker-dark test with explicit port + health poll (8be1c5f) |

## 2. Roadmap traceability (31 changes)

SHIPPED: payment tiers (tiers 1–2 real; CONTRACT_CREDIT ledger + OFF_PLATFORM flow + UI selector outstanding — matches roadmap's own honesty notes), T&C signup+per-job, negotiation/ancillary/confirm-terms/docs, EIR seal+delivery capture, capacity incl. line-item-aware `jobUnitCount`, reliability/chargeback/IP/cancellation-fee, 7-bucket disputes + real SPLIT, RTA/haulage docs, GIT insurance (broker dark), WhatsApp two-way + sessions, DRIVER_ASSOCIATE full flow, compliance engine (8 rules seeded; comment says "five" — stale comment), line items, iOS 4 fixes, forwarder/broker/owner-operator backend, platform_fees + 2 live lines, ledger hash-chain + approvals, GCC endpoints + country phone, stops + lane quote, EDI stub (as claimed).
PARTIAL (honest gaps, match roadmap): `payment_reliability_score` column exists but **no gating reads it anywhere**; register UI offers only SHIPPER/CARRIER (new roles API-only); 14 RTL-hardcoded pages remain beyond the 3 done; monetization lines beyond cancellation/priority are scaffold-only; GCC TRN/licence stay AE-only.
BLOCKED as specified: Change 23 EDI depth (no customer demand).
NOT CODE (correctly): Change 4 social accounts, Change 5 legal docs (lawyer), Change 1 visual redesign (mockup `docs/UI_MODERNIZATION_MOCKUP.md` awaiting sign-off).

## 3. Dead points (confirmed, high confidence)

- Dead modules/exports: `lib/currency.js`, `lib/auditChain.js` (audit.routes re-implements sha256), `lib/permissions.js` + `requirePermission`/`revokeAllSessions` (no callers), `lib/sentry.js captureMessage/isEnabled`, `lib/totp.js currentCode`, `lib/apiResponse.js ERROR_CATALOG/sendErrorCompat`, `lib/hsm.js sha256/sign`, `lib/storage.js putObject`, `lib/email.js sendEmail/isConfigured`.
- Dead endpoints (no web/test/e2e caller; some documented): `fleet/overview`, `me/export`, `me DELETE`, `invoices/print.js` (suspicious `.js` suffix), stripe connect onboard/status (dark), ml ingest ais/noaa, telematics logs, rotate-key/detention-alarms/dispute-sla-check (cron-ops?), `approvals/:id/reject` (no test — add one), EDI consignment reads, ledger instruments, doc-templates, docs.routes.
- Dead frontend: `auth.role*` i18n keys, `JobStatusTracker` import in JobDetail, copy-paste unused import headers in all 10 admin tabs.
- Dead-but-harmless: `prisma/` (referenced nowhere), TODO-1..4 historical labels (honest dark-mode notes, not false claims), stale `compliance_rules "five"` comment.
- All 35 route files mounted; zero missing `require`s; all session tables/columns verified read+written.
- Docs debt: `openapi.yaml` + `API.md` miss all Phase 7/8 endpoints (§22–23 of traceability); `API.md §15` documents obsolete "exactly 3 photos" EIR rule.

## 4. Adversarial notes (HIGH-risk paths)

- Money-before-move gate requires FUNDED (not HELD) — correct; PAY_ON_DELIVERY escrow deferred into shared `confirmDelivery` so WhatsApp + web can't drift.
- `verifyChain` assumes linear history; concurrent same-prev writes (impossible on SQLite single-writer, possible on Postgres) report as breaks — documented single-writer assumption, acceptable.
- `chargeFee` idempotent per key; cancellation/priority wired; fee collection rails still manual (stated).
- Approvals: self-confirm/reject 403, execution atomic with status flip, all steps audited. Dispute-resolve + mark-transferred NOT yet gated behind approvals (existing tests depend on direct paths) — migrate behind setting as follow-up.
- Direct-assign reuses `awardJob` (escrow/ledger/capacity identical); one-hop enforced both directions; broker spread recorded; forwarder needs no roster (by design — any verified carrier).
- Webhook: unknown senders inert (guarded by sender+job checks); location/delivery writes require resolved job.
- Residual: broker POST-job + award flow inherits SHIPPER guards with shipper_id=broker — correct scoping.

## 5. Test evidence

Batched full suite green (70+ tests): core-loop, negotiation, tiers, capacity, disputes, compliance ×2, whatsapp ×2, associate (7), insurance (4, incl. real broker-dark), broker (4, incl. no-bid), ledger-fees-approvals (2), expansion (3), job-line-items, payments, ledger, reconciliation, product-gates, seats, encryption, hardening, local-transport, onboarding, prerender. `tsc --noEmit`, openapi parse, `vite build` green in CI.

## 6. Follow-ups (ordered)

1. Reject-path test for `approvals/:id/reject` (only untested new endpoint).
2. Migrate dispute-resolve financial effects + mark-transferred behind two-person approvals.
3. Register UI for FORWARDER/OWNER_OPERATOR/BROKER (currently API-only).
4. `payment_reliability_score` gating or remove column claim.
5. openapi.yaml/API.md coverage for Phase 7/8 endpoints; fix EIR §15.
6. RTL remaining 14 pages; real-device iOS pass; Meta approval; broker signing; EDI on demand.
7. Prune dead modules/endpoints or mark `@deprecated` (currency, auditChain, permissions, hsm helpers, fleet/overview, me/export).
